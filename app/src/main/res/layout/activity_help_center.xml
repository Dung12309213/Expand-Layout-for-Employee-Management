<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- ===== PHẦN HEADER ===== -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/profile_header"
        android:layout_width="match_parent"
        android:layout_height="91dp"
        app:layout_constraintTop_toTopOf="parent">

        <!-- Tiêu đề "Help Center" -->
        <TextView
            android:id="@+id/textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="44dp"
            android:fontFamily="@font/vollkorn"
            android:text="@string/title_help_center_header"
            android:textColor="@color/textcolor"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <!-- Nút quay lại -->
        <ImageButton
            android:id="@+id/imageButton2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="32dp"
            android:background="@android:color/transparent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:srcCompat="@drawable/ic_backbutton" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- ===== TAB CHUYỂN ĐỔI: FAQ | CONTACT ===== -->
    <com.google.android.material.tabs.TabLayout
        android:id="@+id/tabLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toBottomOf="@id/profile_header"
        app:tabIndicatorColor="@color/brown"
        app:tabSelectedTextColor="@color/brown"
        app:tabTextColor="#888888" />

    <!-- ===== NỘI DUNG TAB: FAQ ===== -->
    <LinearLayout
        android:id="@+id/faq_layout"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:orientation="vertical"
        android:paddingTop="8dp"
        app:layout_constraintTop_toBottomOf="@id/tabLayout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Ô tìm kiếm câu hỏi -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginStart="20dp"
            android:layout_marginEnd="20dp"
            app:cardCornerRadius="24dp"
            app:cardElevation="2dp">

            <EditText
                android:id="@+id/search_input"
                android:layout_width="match_parent"
                android:layout_height="40dp"
                android:background="@android:color/transparent"
                android:fontFamily="@font/nunito_medium"
                android:hint="Search"
                android:paddingStart="16dp"
                android:paddingEnd="16dp" />
        </androidx.cardview.widget.CardView>

        <!-- Danh sách nút lọc danh mục câu hỏi (All, General, Services, ...) -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingVertical="8dp"
            android:paddingStart="16dp"
            android:paddingEnd="16dp">

            <LinearLayout
                android:id="@+id/filter_container"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical" />
        </HorizontalScrollView>

        <!-- Danh sách hiển thị các câu hỏi thường gặp (FAQ) -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/faq_recycler"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginHorizontal="20dp"
            android:layout_weight="1" />
    </LinearLayout>

    <!-- ===== NỘI DUNG TAB: CONTACT US (ẩn ban đầu) ===== -->
    <!-- NỘI DUNG TAB: CONTACT US -->
    <LinearLayout
        android:id="@+id/contact_layout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:visibility="gone"
        android:padding="16dp"
        app:layout_constraintTop_toBottomOf="@id/tabLayout"
        app:layout_constraintStart_toStartOf="parent"
        >

        <!-- Customer Service -->
        <include layout="@layout/item_contact_card"
            android:id="@+id/contact_customer_service"
            android:tag="Customer Service" />

        <!-- Website -->
        <include layout="@layout/item_contact_card"
            android:id="@+id/contact_website"
            android:tag="Website" />

        <!-- Facebook -->
        <include layout="@layout/item_contact_card"
            android:id="@+id/contact_fb"
            android:tag="Facebook" />

        <!-- Instagram -->
        <include layout="@layout/item_contact_card"
            android:id="@+id/contact_instagram"
            android:tag="Instagram" />
    </LinearLayout>


</androidx.constraintlayout.widget.ConstraintLayout>
